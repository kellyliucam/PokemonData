**How to view the project**

Since this project is built with HTML, CSS, and JavaScript, the HTML files will not display as a live webpage directly on GitHub.  

To view the website:  

1. Download the repository to your computer 
2. Open the file in a web browser 
3. Make sure the `js/` folder is in the same directory so the JavaScript functions load correctly  
